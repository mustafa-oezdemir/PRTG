apiVersion: 1

datasources:
  - name: 'PRTG'
    uid: 'prtg'
    type: 'maxmarkusprogram-prtg-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      path: '${PRTG_PATH}'
      cacheTime: ${PRTG_CACHE_TIME}
      timeZone: '${TIMEZONE}'
    secureJsonData:
      apiKey: '${PRTG_API}'
