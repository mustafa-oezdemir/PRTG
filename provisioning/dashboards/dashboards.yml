apiVersion: 1

providers:
  - name: 'PRTG'
    orgId: 1
    folder: 'PRTG'
    folderUid: 'prtg'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/prtg
